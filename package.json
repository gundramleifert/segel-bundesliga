{
  "name": "sbl",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:list": "playwright test --list",
    "typecheck": "web/node_modules/.bin/tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
