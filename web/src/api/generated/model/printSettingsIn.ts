/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Deutsche Segel-Liga API
 * Backend for the Deutsche Segel-Liga website: master data, standings, pairing lists. Points are never stored, only computed from raw results.
 * OpenAPI spec version: 0.1.0
 */

/**
 * How this event's pairing list is printed (Story B-3).
 *
 * Left alone, the sheet is printed the way its configuration implies — the sizes the
 * league's own matchdays have been printed at for years (``app/pairing/pdf.py``). The
 * organizer overrides them because they know the venue, the printer and the paper, and
 * nobody else does.
 */
export interface PrintSettingsIn {
  /** Points. Empty means derived from flights × races per flight. */
  font_size?: number | null;
  /** Rotate the sheet; a wide fleet reads better across it. */
  landscape?: boolean;
  /** One page per team after the overview. */
  team_pages?: boolean;
}
